import 'package:flutter/cupertino.dart';

class BinaryNums extends StatelessWidget {
  // const BinaryNums({Key? key}) : super(key: key);
  const BinaryNums({Key? key}): super (key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}